{
  "name": "bc_id",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "truffle compile",
    "migrate": "truffle migrate --network development",
    "migrate:reset": "truffle migrate --reset --network development",
    "deploy:local": "truffle migrate --reset --network development",
    "test": "truffle test",
    "test:local": "truffle test --network development",
    "console": "truffle console --network development",
    "simulate": "truffle exec scripts/simulate.js --network development"
  },
  "devDependencies": {
    "truffle": "^5.11.5"
  }
}
